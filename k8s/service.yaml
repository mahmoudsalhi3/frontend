apiVersion: v1
kind: Service
metadata:
  name: minolingo-frontend
  labels:
    app: minolingo-frontend
spec:
  type: NodePort
  selector:
    app: minolingo-frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30081
